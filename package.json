{
  "name": "integracion",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && mkdirp dist/functions && mkdirp dist/frontend && npm run build:backend && run build:frontend",
    "build:backend": "",
    "build:frontend":"cd frontend && npm install && env-cmd -f ../react.env npm run build && mv build /* ../dist/frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mkdirp": "^1.0.4",
    "rimraf": "^3.0.2"
  }
}
